```{=html}
<ul>
    <% for (const item of items) { %>
        <li>
            <%= item.author %>. (<%= item.date %>).  <i><%= item.title %></i> [Conference session]. <%= item.conference %>, <%= item.location %>. ISBN:  <%= item.isbn %>
        </li>
    <% } %>
</ul>
```
