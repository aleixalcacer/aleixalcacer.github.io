```{=html}
<ul id="quarto-proceeding">
    <% for (const item of items) { %>
        <li class="quarto-publication">
            <%= item.author %> (<%= item.year %>). <%= item.title %>. In <i><%= item.booktitle %></i>
            <% if (item.series) { %>
                (<%= item.series %>)
            <% } %>
            . <%= item.publisher %>.
            <a href=https://doi.org/<%= item.doi %>> <%= item.doi %> </a>.

            <div class="quarto-labels">
                <% if (item.code) { %>
                    <span class="quarto-label"><a href="<%- item.code %>"> CODE </a></span>
                <% } %>
                <% if (item.pdf) { %>
                    <span class="quarto-label"><a href="<%- item.pdf %>"> PDF </a></span>
                <% } %>
            </div>
        </li>
    <% } %>
</ul>
```
